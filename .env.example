# ==============================================================================
# Task Manager Frontend - 環境変数設定ファイル（テンプレート）
# ==============================================================================
# このファイルは Git 管理対象です。機密情報は含めないでください。
# 実際の開発時は、このファイルを `.env` にコピーして使用してください。
#
# コピーコマンド:
#   cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# API設定
# ------------------------------------------------------------------------------
# バックエンドAPIのベースURL
# - Docker環境では `http://host.docker.internal:8000` を使用
NEXT_PUBLIC_API_BASE_URL=http://host.docker.internal:8000

# ------------------------------------------------------------------------------
# NextAuth設定
# ------------------------------------------------------------------------------
# NextAuthのセッション暗号化シークレット
# - 32文字以上を設定
# - 生成コマンド: openssl rand -base64 32
NEXTAUTH_SECRET=

# ------------------------------------------------------------------------------
# 暗号化設定
# ------------------------------------------------------------------------------
# トークン暗号化キー（64文字の16進数）
# - バックエンドの ENCRYPTION_KEY と同一の値を設定
# - 生成コマンド: openssl rand -hex 32
ENCRYPTION_KEY=

# ------------------------------------------------------------------------------
# OAuth2プロバイダー設定
# ------------------------------------------------------------------------------
# Google OAuth2 クライアントID
# - バックエンドの GOOGLE_CLIENT_ID と同一の値を設定
# - リダイレクトURI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=

# Google OAuth2 クライアントシークレット
GOOGLE_CLIENT_SECRET=
